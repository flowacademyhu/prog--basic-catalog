const unio = require('./union');
const copy = require('./copy');
const sep = require('./separate');
const sumArrayFor = require('./summary');
const selectEvens = require('./selection');

module.exports {
  unio: unio,
  copy: copy,
  sep: sep,
  sumArrayFor: sumArrayFor,
  selectEvens: selectEvens
};
